# ChapterA

付録A用のサンプルコードです。※付録Aは電子版のみに含まれます。

「Dev Containers: Rebuild and Reopen in Container」を実行した後は、以下のようにAWSの認証情報を環境変数に追加するなどしてからCDKを実行してください。

```bash
export AWS_REGION=ap-northeast-1
export AWS_ACCESS_KEY_ID=XXXXX
export AWS_SECRET_ACCESS_KEY=XXXXX
export AWS_SESSION_TOKEN=XXXXX
```
